{
  "name": "siwe-auth-task",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "MIT",
  "scripts": {
    "api:prisma:generate": "pnpm --filter ./packages/api prisma:generate",
    "api:build": "pnpm --filter ./packages/api build",
    "api:start": "pnpm --filter ./packages/api start",
    "api:dev": "pnpm --filter ./packages/api start:dev",
    "api:test": "pnpm --filter ./packages/api test",
    "api:test:watch": "pnpm --filter ./packages/api test:watch",
    "test-frontend:start": "pnpm --filter ./packages/test-frontend start",
    "test-frontend:build": "pnpm --filter ./packages/test-frontend build"
  },
  "workspaces": [
    "packages/*"
  ]
}
